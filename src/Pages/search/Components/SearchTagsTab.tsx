import '../index.css';

const SearchTagsTab = ({ searchWord }: { searchWord: string }) => {
  return (
    <div id="search-tags">
      <h1>Searching tags...</h1>
    </div>
  );
};

export default SearchTagsTab;